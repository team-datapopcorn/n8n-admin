// 서버 목록은 이제 lib/server-config.ts의 getServers()로 동적으로 가져옵니다.
// 하드코딩된 SERVER_META, SERVER_LABELS, SERVER_IDS는 제거되었습니다.
// 페이지 컴포넌트에서 getServers()를 사용해 서버 이름/URL을 가져오세요.

// (이 파일은 향후 공통 상수 추가 시 사용)
export {}
