{
  "buildCommand": "cd admin && pnpm build",
  "outputDirectory": "admin/.next",
  "installCommand": "cd admin && pnpm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/auto-cleanup",
      "schedule": "0 18 * * *"
    }
  ]
}
