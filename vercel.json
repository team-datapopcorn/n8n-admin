{
  "buildCommand": "cd admin && pnpm run build",
  "outputDirectory": "admin/.next",
  "installCommand": "npm install -g pnpm@10.28.2 && cd admin && pnpm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/auto-cleanup",
      "schedule": "0 18 * * *"
    }
  ]
}
